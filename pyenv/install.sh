#!/bin/bash
#
# Run the pyenv installer which will download pyenv, pyenv-virtualenv and other
# default plug-ins to ~/.pyenv folder.
curl -L https://raw.githubusercontent.com/yyuu/pyenv-installer/master/bin/pyenv-installer | bash
