t.Parallel()
tests := []struct{
	msg     string
	wantErr string
}{
	{
		msg:     "ok",
		wantErr: "",
	},
}

for _, tt := range tests {
	tt := tt
	t.Run(tt.msg, func(t *testing.T) {
		t.Parallel()
		{{++}}
	})
}
